const swiper = new Swiper('#header__carousel', {
    // Optional parameters
    loop: true,

    // If we need pagination
    pagination: {
        el: '.header__carousel-pagination',
        clickable: true,
    },
});



